{
  "ConnectionStrings": {
    "connStr": "User ID=postgres;Password=postgres;Server=localhost;Port=5432;Database=CatalogDb; Integrated Security=true;Pooling=true;"
  },

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}

//docker ps
//docker exec -it containerID bash
//psql -U postgres    
// \l per la lista dei db (q per uscire)
// \d per le relazioni